# URL da API. OBRIGATÓRIO antes de "npm run build" se o app for aberto em outra origem que não a API.
# Ex.: API em https://localhost:44362 → use:
# VITE_API_URL=https://localhost:44362
#
# Sem isso, após build + login a foto de perfil pode não carregar (cache ou origem errada).
# Se você servir o build pela própria API (mesma origem), pode deixar vazio.
VITE_API_URL=
